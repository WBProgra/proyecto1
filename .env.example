# ===================================================
#          CONFIGURACIÓN DEL BACKEND (DJANGO)
# ===================================================

# -- Configuración General de Django --
# ¡IMPORTANTE! Cambia esto en producción por una clave secreta y única.
# Puedes generar una aquí: https://djecrety.ir/
SECRET_KEY=django-insecure-fallback-key-for-development

# Modo Debug. Cambiar a 'False' en producción.
DEBUG=True

# Hosts permitidos, separados por comas. Ejemplo: 'localhost,127.0.0.1,mydomain.com'
ALLOWED_HOSTS=localhost,127.0.0.1

# Orígenes de confianza para CSRF, separados por comas.
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173


# -- Configuración de la Base de Datos (PostgreSQL) --
# Si estas variables no se definen, el proyecto usará una base de datos SQLite local.
# Descomenta y rellena las siguientes líneas para usar PostgreSQL.
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432


# ===================================================
#          CONFIGURACIÓN DEL FRONTEND (REACT)
# ===================================================

# URL del backend a la que el frontend debe conectarse.
# Asegúrate de que no termine con una barra (/).
VITE_BACKEND_URL=http://127.0.0.1:8000
